apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-home
  namespace: devsecops
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: managed-premium
  volumeMode: Filesystem

